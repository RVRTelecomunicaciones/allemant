import 'package:flutter/material.dart';

class LevelPage extends StatelessWidget {
  const LevelPage({Key key}) : super(key: key);
    static const routeLevelName = '/level_page';


  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}