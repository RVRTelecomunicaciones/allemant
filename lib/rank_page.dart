

import 'package:flutter/material.dart';

class RankingPage extends StatelessWidget {
  const RankingPage({Key key}) : super(key: key);
    static const routeRankingName = '/rank_page';


  @override
  Widget build(BuildContext context) {
    return Container(
    
    );
  }
}